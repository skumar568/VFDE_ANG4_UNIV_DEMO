/**
 * Book entity, used for filtering as well.
 */
export class Notification {
  /**
   * @type {String} from Unique String identifier.
   */
  from: String;
   /**
   * @type {string} role
   */
  role: String;

  /**
   * @type {string} date
   */
  date: String;

  /**
   * @type {title} title.
   */
  title: String;
  /**
   * @type {description} description.
   */
  description: String;
}
